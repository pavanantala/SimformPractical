@model SimformWeb.Models.EmployeeToProjectEntity

@{
    ViewBag.Title = "AddEditEmployeeToProject";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>AddEditEmployeeToProject</h2>

@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>EmployeeToProjectEntity</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">
            @Html.LabelFor(model => model.EmpId, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownListFor(m=>m.EmpId, (IEnumerable<SelectListItem>)ViewBag.Employees, new { id = "EmpId", @class = "form-control" })

                @*@Html.EditorFor(model => model.EmpId, new { htmlAttributes = new { @class = "form-control" } })*@
                @Html.ValidationMessageFor(model => model.EmpId, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.ProjectId, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownListFor(m =>m.ProjectId, (IEnumerable<SelectListItem>)ViewBag.Projects, new { id = "ProjectId", @class = "form-control" })

                @*@Html.EditorFor(model => model.ProjectId, new { htmlAttributes = new { @class = "form-control" } })*@
                @Html.ValidationMessageFor(model => model.ProjectId, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "EmployeeToProjectIndex", "Employee")
</div>
